"""
Transformer decoder-only model: GPT

"""
from typing import Sequence, Tuple

from torch import Tensor


# TODO: complete this!

def preprocess(input_ids: Sequence[Tuple[int, int, int, float]]) -> Tensor:
    """

    :param input_ids: a sequence of encoded Tuple(pitch: int, duration: int, interval: int, velocity: float)
    :return:
    """
    pass

